name: ai_response_selector
description: A Flutter package for converting AI responses to multiple select options with easy integration and comprehensive state management.
version: 1.0.0
homepage: https://github.com/rudSarkar/ai_response_selector

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
